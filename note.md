# 生成式软件工程
这是一门面向新时代生成式人工智能的实验性软件工程课程

大学能教你的
- 信息差
- 能坐牢
- **能驾驭**

LLM: 一个概率分布 Pr[token | context]

使用者的想象力限制了 Agent 的上限

跑出来，不等于能完成，应该学会设计一个合理的架构，而不是对着需求写

三个空间
- Intent Space: 需求的提出，不能100%确定
- Spec Space: 从 Intent 到 Spec 的映射会曲解
- Impl Space: 计算机程序只接受100%无歧义的指令

一个 Intent 可以映射到多个 Spec，一个 Spec 可以映射到多个 Impl，但是 Impl 是唯一的，三者中存在 gap，这就是困难所在

模型如何补 gap\
猜，模型很容易立即遵循指令\
正确的软件工程实践应该先讨论一下

软件工程研究“怎么让工程不失败”，把一件大事，在人和工具的协作下完成，有技术，也有人类学、社会学、管理学的部分

LLM 时代，失败的定义没有变\
失败 = 软件不满足 intent 和 constraints\
真实 intent 和 真实 spec 都非常困难，gap 的自由度很高

这是一门实验性课程，可能在课程还没结束的时候就过时，被 Scaling Laws 爆杀，但是了解这些概念对工程实践也不是完全没用。

提示词工程到上下文工程
- 提示词工程关系一句话怎么写
- 上下文工程关系每次决策前哪些 token 以什么来源、顺序和作用域出现

AI 降低工程快速启动的成本

可验证：越明确、越可验证的要求，越能得到理想的结果

rule of thumb: 先把需求和约束写清楚，再去实现\
要么完成，要么显式失败

不要将品味留给默认值，要压缩合法解空间

Intent -> Spec - > Impl
- 明确意图需要**产品领域**的知识
- 把意图变成设计，需要**软件工程**的知识
- Impl 变便宜，判断没有

Pr[token | context] 计算深度有限，一次前向计算不能完成复杂任务，但是通过利用上下文思考，可以实现。

上下文是争用资源

在大型复杂软件系统中，self-attention 可以看到好的 practice，学习并遵循已有的 best practice\
代码库本身也是提示词

仍然需要对实现的东西有概念，只能通过学习的方式获得，无法驾驭自己不理解的东西

创造力需要正确的概念和理解\
对世界建立正确的概念和理解，是具有发散和创造能力的前提

“上课耽误学习”的本质\
课外的信息量、信息密度巨大，但从未教过如何用好

### 留意：阅读与学习
virtually implement
- 看到问题，先问自己：我会怎么想？如何 approach?
- 再看作者的做法
    - 如果一致：很好，掌握了这个概念
    - 如果不一致：这就是学习的机会

对细节保持注意力
- 读东西要有“细节感知”的意识
- 预测下一段是什么
- 留意使你持续学习，提升水平

软件系统约定俗成的 Best Practice 是经验的产物

项目 = 由 Atomic Change 推进的快照

文件形式的接口对人类和工具友好，但是丢失不适合文件表达的信息，如 traceability\
所以就有了 git

学习的目的：形成自己的理解 (first principles)

git 组织了文件、目录、快照之间的关系\
还可以应对人类的并行开发和冲突合并

从设计上，git 以 commit 为单位，对于 rebase 操作，如果冲突，rebase 后的 commit id 是不一样的，显然存在不和谐

如果用 change 作为单位，显然更 human-friendly，这就是 Jujutsu 的设计理念，甚至不需要 staging area

一种高效的学习方式是，理解正确的设计，再回望历史，理解是如何发展过来的

由于 AI agent 的代码生成能力，git 的设计已不适合 Agent 的协作\
也许一切需要重新设计适合 Agent 的工具

git 有个好用的功能是 git worktree，允许在同一个 repo 下有多个工作区，方便不同的分支并行开发

人的限制，塑造了工具

版本控制只是工具，软件工程真正的困难在于团队管理\
人数增加，沟通成本平方级上升

启发
- 项目不仅是当前状态
- 记住历史，并行探索，汇合结果
- 失败也是经验

Minimum Viable Product (MVP)：最小版本验证，仔细考虑架构

早期的 Prompt 对项目的影响最大，任何确定的设计都有可能是潜在的 technical debt，后续的修改成本可能很高

软件工程需求高度定制化，高度需要人类智慧,且融合多种因素的权衡\
进度、成本、质量可能会一起失控

在成本、时间等约束下交付软件，将人的意图变成可工作的软件系统

瀑布模型
- 需求分析 -> 系统设计 -> 系统实现 -> 测试验证 -> 交付维护
- risky, invites failure

Do It Twice (DIT) 模型
- 先做一个快速的原型，验证需求和约束
- 再做一个可交付的版本，验证架构和设计
- MVP: 先做一遍，从中学习，再完善产品

敏捷开发
- 迭代式开发，快速交付，持续反馈
- 反复做 Pilot，快速验证需求和约束

TDD
- 拿 TestCase 当需求和设计

软件工程：为每个阶段创建方法学和工具，尽可能减少返工概率\
不同阶段需要不同的“语言”
- 需求分析：文档、用例、界面原型
- 系统设计：实体关系、状态迁移、模块和接口
- 代码实现

UML (Unified Modeling Language)
- 中间语言，用于在不同阶段之间传递信息
- 通过定义模型元素和语义，最大程度减少在没有代码时的语义漂移
- 可能已经死了，但是设计时候的 first principles 仍然有价值
